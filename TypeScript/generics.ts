let arr=['apple',1,2,true,false,()=>{return 'hello'},10.123,new Date()];

console.log(arr);

let arr1:Array<number>=[1,2,2,3,3,4,4,5,5,];// here array can hold only used for number date

console.log(arr1);

let arr2:Array<string>=['Hanu','Somu','Giri'];//here array can hold only u/ here array can hold only used for number datesed for number date
for (const i of arr2){
    console.log(i);
}